# ACCE GPU Assignment 1

Rainwater flooding simulation (sequential + CUDA).

## Reproduce results

```sh
./run_all_tests.sh        # runs every test_files/*.in on a GPU node,
                          # checks correctness, reports speedups
```

Outputs are written to `test_outputs/` and a summary to `test_outputs/summary.txt`.
